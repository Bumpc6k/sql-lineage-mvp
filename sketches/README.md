# 界面原型（sketches/）—— 两轮共 6 个变体

> 这里全是**一次性原型**（throwaway）：纯静态 HTML，用假数据，只为定方向、给别人看。
> **不要**从这里拷贝代码进 `apps/web`；方向定了以后，正式实现按 `apps/web/README.md` 的工程约定来。

## 怎么打开

```bash
bash ops/serve-sketches.sh          # 起静态服务（:5180），然后浏览器打开下面任一条
```

| 轮次 | 变体 | 在线地址 | 一句话 |
| --- | --- | --- | --- |
| 第 1 轮（09-20） | `v1-dark-tech` | http://localhost:5180/v1-dark-tech/ | 深色科技：**AI 对话 + 知识大盘**融合单屏 |
| 第 1 轮 | `v2-enterprise-light` | http://localhost:5180/v2-enterprise-light/ | 同一概念，浅色企业控制台 |
| 第 1 轮 | `v3-minimal-focus` | http://localhost:5180/v3-minimal-focus/ | 同一概念，极简单栏 |
| 第 2 轮（09-21） | `001-dense-tool` | http://localhost:5180/001-dense-tool/ | 工业工具风：**三栏高密度**，左导航 + SQL 编辑器 |
| 第 2 轮 | `002-airy-editorial` | http://localhost:5180/002-airy-editorial/ | 清爽编辑风：单列卡片，**三段同屏**（链路/字段/口径） |
| 第 2 轮 | `003-graph-first` | http://localhost:5180/003-graph-first/ | 图谱探索风：**画布为中心**，点节点高亮链路 + 右侧详情抽屉 |

截图归档：`_shots/`（A/B/C = 第 2 轮；OLD-* = 第 1 轮）。


## 第 3 轮（09-22）：对话优先首屏（P1 原型，3 个立场）

按《规划 v1.1》的「对话是入口 + Evidence-first」做，三版都用同一条演示问答（`ads.ads_产销存月报` 的产量怎么来的），便于对比。

| 变体 | 在线地址 | 立场 | 一句话 |
| --- | --- | --- | --- |
| `100-chat-minimal` | http://localhost:5180/100-chat-minimal/ | 极简对话（大模型式） | 只有会话列 + 居中消息流；凭证是消息里的卡片；最像 ChatGPT，学习成本最低 |
| `101-chat-workbench` | http://localhost:5180/101-chat-workbench/ | 工作台三栏（深色 Copilot） | 窄导航 + 会话列 + 对话 + 右侧「会话上下文」常驻（引用的表/口径/调用/审计） |
| `102-chat-evidence-first` | http://localhost:5180/102-chat-evidence-first/ | 凭证优先（左对话/右证据画布） | 右侧是**证据画布**（血缘图/口径/报告三个 tab），点左侧结论即可切到对应证据 |

三版共同点（P1 硬要求）：工具调用步骤条可展开看真实请求与耗时、结论带 `status`（verified/置信度/版本/来源行）、**凭证卡片**（血缘图/口径详情/HTML 报告）、无 LLM key 时以规则模式运行的提示、建议追问。

截图归档：`_shots/CHAT-*.png`　架构图：`docs/platform-vision/diagrams/architecture.html`（另附同目录 PNG）

## 两轮的差别（关键）

| | 第 1 轮 v1/v2/v3 | 第 2 轮 001/002/003 |
| --- | --- | --- |
| 核心隐喻 | 「**AI 助手问答** + 知识大盘」：问一句 → 生成链路 SQL → 卡片式回答 | 「**查询工具**」：贴 SQL → 看血缘结果 → 打开报告 |
| 功能落点 | 口径卡片、分层分布、链路健康、校验徽章（**字段血缘只有一个数字**） | **真表格**的字段级血缘（目标/来源/表达式/口径四列）、按层展开的上游/下游、报告浏览屏 |
| 可交互 | 几乎无（按钮是装饰、SQL 被 `...` 截断） | 有：字段过滤、分段切换、点节点高亮链路、上下游切换、报告列表/内嵌预览 |
| 两屏 | 单屏融合（报告只是一句「查看完整报告」） | 明确两屏：**血缘查询** + **报告浏览** |
| 适合 | 讲「我们要做 AI 问数 + 数据资产运营」的**故事** | 讲「这东西现在就能用」的**工具** |

## 第 2 轮三个变体的立场对比

| 维度 | 001 工业工具风 | 002 清爽编辑风 | 003 图谱探索风 |
| --- | --- | --- | --- |
| 信息密度 | 高（三栏同屏） | 中（单列分节，往下读） | 中（图为主，细节进抽屉） |
| 主视觉 | SQL 编辑器 + 结果表 | 大标题 + 卡片流 | 血缘画布 |
| 核心动作可见性 | 高（一屏看到全部） | 中（要滚） | 高（点一下就有反馈） |
| 扫读性 | 高（工程师友好） | 最高（领导/产品友好） | 中（图好懂，细节要展开） |
| 演示冲击力 | 中 | 中 | **高**（高亮链路+光晕，讲"改一个字段影响谁"很直观） |
| 弱点 | 小字多、偏"后台味"、窄屏吃力 | 结果分散在三段，要滚动才能看全 | 表格细节弱、窄屏/小屏体验差 |
| 最适合 | 天天用的数据开发 | 汇报、评审、给业务方看 | 演示、讲影响面、做作品集封面 |

## 我的建议（不是折中，是有取舍的）

**骨架用 `002` 的排版（浅底、卡片、三段同屏），结果区引入 `003` 的血缘画布（节点高亮 + 点开详情），口径/知识部分沿用第 1 轮的"知识大盘"卡片。**

理由：
- `001` 的三栏在 1440px 以下就挤，且与海豚自带的深色 UI 撞色**反而**不好认（我们是嵌在它旁边的独立页，浅色更"像新东西"）；
- `003` 的图是这台系统**最有说服力**的表达（"改一个字段影响谁"一目了然），但整屏只有图会让"字段级血缘"这个硬能力退到抽屉里；
- 第 1 轮的「知识大盘」（75 条口径 / 298 字段 / 分层分布 / 链路健康）是很好的**第二屏**，别丢，但现在不该挤进查询屏。

## 下一步怎么走（选一个）

- **A**：按上面的混合方案，让原型变成 3 屏（血缘查询 / 报告浏览 / 知识大盘），再截图对比一轮
- **B**：挑一个变体定稿，我把它写进 `apps/web`（正式实现，含契约对齐的用例）
- **C**：先不选，你自己把 6 个都点一遍（`bash ops/serve-sketches.sh` 后打开上表地址），再告诉我改哪儿
- **D**：装 Node，把 `apps/web` 升级成 Vite + TS 工程，之后原型就在正式工程里迭代

---

## 第 4 轮 · 定稿原型（收敛：对话优先 + Evidence-first）

> **`200-chat-final/`** — 由用户拍板的收敛版本：以 `102-chat-evidence-first` 为骨架，把 `101-chat-workbench` 的右栏「会话上下文」合并进来做成**常驻**，`100-chat-minimal` 的极简风留作后续「轻量模式」。

打开：`bash ops/serve-sketches.sh` → <http://localhost:5180/200-chat-final/>

| 屏 | 深链接 | 内容 |
| --- | --- | --- |
| ① 对话工作台 | `/200-chat-final/` | 窄导航 + 会话列 + 对话区（工具步骤可展开看真实请求与耗时 → 结论带 `verified/置信度/版本/来源行` → 三张凭证卡 → 建议追问）+ **右面板**（血缘图 / 口径 / 报告 / 上下文 四个 tab + 常驻引用条） |
| ② 血缘中心 | `/200-chat-final/?page=lineage` | 全链路血缘（ods→dwd→dws→ads + 维表，10 节点 9 边，零交叉）+ 右侧表详情（字段/相关口径/凭证）+ 影响分析 |

**合并方式（关键设计决策）**：`101` 的右栏「会话上下文」不新开一栏（三栏会让对话区太窄），而是拆成两层——**常驻一行引用条**（本次会话引用了哪些表/口径/报告/调用，一眼可见、点开即切面板）＋ **「上下文」tab**（完整的表列表 + 命中口径 + 凭证清单 + 调用与审计）。这样证据画布仍然占满右栏，上下文也不丢。

截图：`_shots/FINAL-1-chat.png`、`_shots/FINAL-2-lineage.png`

**自检（真浏览器 + 几何计算，非目测）**：对话屏 4 节点/3 边、血缘中心 10 节点/9 边，**连线交叉 0 处、端点贴合 9/9（误差 <2px）**，最后一列未溢出容器。过程中修掉 3 个真 bug：`pick()` 在数据赋值前调用 → 画布空白；血缘中心高亮链传错变量（`CHAIN`→`CHAIN2`，节点 id 体系不同）；节点宽度上限 168px 把最后一列顶出容器 → 改列内居中 + 自适应。
